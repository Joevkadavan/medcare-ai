# MedCare AI — root environment example
# Copy values into each service's own .env (see backend/.env.example)

# Backend
FRONTEND_URL=http://localhost:3000
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
MOCK_AI_ONLY=false
RAG_ENABLED=true
RAG_TOP_K=4

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
